/**
 * se pide representar el algoritmo que nos calcule la suma de los N primeros numeros pares a partir de N.
 * es decir, si insertamos un 5, nos haga la suma de 6+8+10+12+14.
 */

const prompt = require("prompt-sync")();

let N = Number(prompt("Introduce N: "));